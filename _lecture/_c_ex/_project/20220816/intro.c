#include <stdio.h>  // 입출력 전처리기 불러오기, 직접만든 헤더파일인 경우 ""사용하는 경우도 있음

//int sum(int value1, int value2) {
//	return value1 + value2;
//}

//int Sum(int value1, int value2);  // 함수 원형 선언, 피호출 함수가 아래에 있어도 가능하도록

void main(void){  // void, 반환값, 입력값 없음, 모든 코드는 main 안에서, int로 반환하는 사람도 있음 return 0 필요
	
	//printf("Hello World!!\n");  // 불친절한 C, 개행도 직접
	//printf("Hugh\n");

	/*int a = 100, b = 200, result = sum(a, b);
	printf("100 + 200 = %d\n", result);*/

	/*int money;
	money = 3000;
	printf("money에 저장된 값은 %d", money);*/

	/*int s;
	s = Sum(2, 3);
	printf("%d", s);*/

}

//int Sum(int value1, int value2) {
//	int result = value1 + value2;
//	return result;
//}